- name: variable perferences 
  hosts: local 
  connection: local 
  # vars:
  #   NAME: "Ansible from PLAY"
  #   vars_files:
  # - course.yaml
  # vars_prompt:
  # - name: NAME
  #   prompt: please enter the name
  #   private: false
  tasks:
  - name: print name
    # vars: 
    #   NAME: "Ansible from TASK"
    ansible.builtin.debug:
      msg: "Hi {{ NAME }}"
  

# 1. argument will run ..it will give the output
#  ansible-playbook -i inventory.ini -e 'NAME="Ansible from args"' 10-vars-prefences.yaml

#2. task will run
# ansible-playbook -i inventory.ini 10-vars-prefences.yaml
# please enter the name: ansible from prompt

#3. file will run 
#4. prompt
#5.play level variables
#6. inventory
