- name: practing condition in ansible
  hosts: local
  connection: local
  vars: 
    NUMBER_OF_SESSIONS: 7
  tasks:
  - name: check the condition is ansible more than 5 sessions
    ansible.builtin.debug:
      msg:"Yes, more than 5 sessions"
    when: NUMBER_OF_SESSIONS > 5