- name: practice conditions
  hosts: local
  connection: local
  vars:
    IS_LIVE: true
  tasks:
    - name: check Is_live or not
      ansible.builtin.debug:
        msg: "Yes, it is Live"
      when: IS_LIVE